# Nexus 版本更新记录

本项目遵循 `0.0.x` 小步版本规则。本记录只写入已经实现并完成验证的内容；尚未完成的工作保留为“进行中”或“待验证”。

## 0.0.11｜Hermes 模型语义修正与外部渠道运行状态

状态：功能实现、完整自动验证、成品构建及独立测试环境无损升级均已完成，待用户真机验收人物默认值与其他渠道“思考中”状态。

已完成：

- 修复 Hermes `/v1/models` 模型语义误判：`parent` 为空表示主推理模型，不再据此判断人物；只有 `kind=persona`、`object=persona` 或 `object=hermes.persona` 等明确元数据才进入人物列表；
- 当前 Hermes 对外声明的主模型名称“星禾”不再显示为人物；升级后清除旧版本错误保存的人物选择，默认恢复为 `Hermes 默认（default）`，聊天请求不发送 `persona_model`；`gpt-5.6-sol` 仍作为调用模型路由保留；
- Gateway 新增其他渠道运行状态观察器，只读调用原版 Hermes `/health/detailed` 与 `/api/sessions` 公开 HTTP API，保守识别 QQ、微信等渠道正在处理的会话；候选不唯一时不猜测，探测失败时清除合成状态；
- Android 可为其他渠道任务显示“思考中”，任务结束后自动刷新消息；外部任务标记为不可停止并仅显示进度指示器，Nexus 自己发起的任务继续显示停止按钮；
- 版本升级到 `0.0.11`，生成 Android Debug APK、Gateway ZIP 与 SHA-256 清单；独立成品测试环境继续只运行构建后的 Gateway ZIP，不读取仓库源码、不运行 Docker。

2026-07-24 自动验证：

- Gateway：完整测试 `95 passed, 9 skipped`；Python `compileall` 与网页 JavaScript 语法检查通过；
- Android：135 项单元测试通过，`0 failures / 0 errors / 0 skipped`；`lintDebug` 为 `0 errors, 1 warning`，唯一警告是按需求允许 HTTP 的预期 `InsecureBaseConfiguration`；Debug APK 构建成功；
- 成品：`Nexus-Android-0.0.11-debug.apk` SHA-256 为 `1ba7cdb613c4d6fe98e460fa0026688493959492944930470399009da2cb539f`；`Nexus-Gateway-0.0.11.zip` SHA-256 为 `83687a296526e51166b2bd497ad17fe8659a09688a59196c605ff7dc96ad6ea3`；
- Gateway ZIP：精确包含 20 个生产文件，完整性、版本和私钥标记检查通过；不含测试、缓存、日志、运行数据、账号、配置副本、Bootstrap Token、证书、私钥、APK 或 Android 构建目录；ZIP 内 Gateway 版本为 `0.0.11`；
- 独立测试环境：无损升级到 `Nexus-Gateway-0.0.11.zip` 后，本机与局域网 `/health` 均返回 Gateway `0.0.11`；部署记录中的 ZIP SHA-256 与成品一致；
- 端口与进程：`18787` 只有一个 listener，`process.json` PID 与实际监听进程一致；旧 `18788` 无 listener；
- 数据保留：普通升级前后，Nexus 账号与上游连接配置副本的存在状态及内容哈希一致，11 个媒体文件与 6 个历史 TLS 文件的数量及组合哈希一致，原有 2 个日志文件均保留；
- 边界确认：按要求未在本机运行 Docker，未修改、安装、升级、停止、重启或以其他方式管理任何 Hermes 文件、配置、数据、虚拟环境或进程。

待验收：

- 用户在 Android 真机打开人物设置，确认默认显示 `Hermes 默认（default）`，不再出现误判的“星禾”；
- 用户从 QQ、微信等其他渠道发送消息时，确认 App 对应会话会显示“思考中”，完成后刷新回答且不出现不可用的停止按钮。
## 0.0.10｜移动端附件上下文与输入体验优化

状态：功能实现、完整自动验证、成品构建及独立测试环境无损升级均已完成，待用户真机验收手机附件处理、侧栏宽度、登录页位置与输入法响应。

已完成：

- Android 在每次聊天请求中声明 Android 手机端能力，明确手机不能直接访问 Hermes 主机本地路径，也不支持桌面拖拽；人物模型、调用模型、推理深度与多附件能力继续保留；
- Gateway 消费 `client_context`，不把 Nexus 内部字段直接透传给 Hermes；仅通过原版 Hermes HTTP API，把移动端约束作为本轮临时系统提示传给原生 Session Chat 或指定模型的 OpenAI 兼容路由；
- 手机文件附件增加来源标记，要求 Hermes 不把 `/tmp/...`、`C:/...`、`sandbox:/...` 等主机路径当作已交付文件；返回文件时直接呈现内容或提供手机可访问的 HTTP/HTTPS 地址，无法交付二进制文件时明确限制并提供手机替代方案；
- 已有系统提示会与移动端提示合并，内部移动端系统标记从公开会话历史中过滤，未携带移动端上下文的旧客户端请求保持不变；
- Android 侧栏进一步收窄，登录信息继续上移；聊天输入改为轻量输入组件，触摸时提前请求焦点并显示键盘，消息列表随输入法和流式回复即时跟随，减少键盘弹出及上浮延迟；
- 版本升级到 `0.0.10`，生成 Android Debug APK、Gateway ZIP 和 SHA-256 清单；独立成品测试环境继续只运行构建后的 Gateway ZIP，不读取仓库源码、不运行 Docker。

2026-07-24 自动验证：

- Gateway：完整测试 `88 passed, 9 skipped`；Hermes 只读边界、本地测试环境与 Docker 配置契约聚焦回归 `19 passed`；Python `compileall`、网页契约和 JavaScript 语法检查通过；
- Android：133 项单元测试通过，`0 failures / 0 errors / 0 skipped`；`lintDebug` 为 `0 errors, 1 warning`，唯一警告是按需求允许 HTTP 的预期 `InsecureBaseConfiguration`；Debug APK 构建成功；
- 成品：`Nexus-Android-0.0.10-debug.apk` SHA-256 为 `ab6d3b16b51fdac8c2a245e348cd63505c340b7eb0cb5a13b07bfe32bfbe4fa3`；`Nexus-Gateway-0.0.10.zip` SHA-256 为 `c5f418d47aadc825b03595b0d248a5079469e480b336053d954e65c8f05f954f`；
- Gateway ZIP：精确包含 20 个生产文件，完整性、版本和敏感信息检查通过；不含测试、缓存、日志、运行数据、密码、Token、Hermes Key、Session Secret、Bootstrap Token、证书、私钥、APK 或 Android 构建目录；ZIP 内 Gateway 版本为 `0.0.10`；
- 独立测试环境：无损升级到 `Nexus-Gateway-0.0.10.zip` 后，`http://127.0.0.1:18787/health` 与 `http://10.0.0.123:18787/health` 均返回 Gateway `0.0.10`；部署记录中的 ZIP SHA-256 与成品一致；
- 端口与进程：`18787` 仅有一个绑定到 `0.0.0.0` 的 listener，`process.json` PID 与实际监听进程一致；旧 `18788` 无 listener；
- 数据保留：普通升级前后，Nexus 账号、上游连接配置副本、9 个媒体文件和 6 个历史 TLS 文件的存在状态、数量及内容哈希一致；原有 2 个日志文件均保留；
- 边界确认：按要求未在本机运行 Docker，未修改、安装、升级、停止、重启或以其他方式管理任何 Hermes 文件、配置、数据、虚拟环境或进程。

待验收：

- 用户在 Android 真机发送图片和文件，确认 Hermes 按手机附件处理，不再要求桌面拖拽、访问主机路径或使用电脑快捷键；
- 用户确认侧栏宽度、登录信息位置、聊天输入法弹出速度以及消息列表随键盘上浮的体验符合预期。

## 0.0.9｜会话级模型控制、多文件上传与 AMOLED 界面

状态：功能实现、完整自动验证、成品构建及独立测试环境无损升级均已完成，待用户真机验收多文件选择和新版对话交互。

已完成：

- Android 附件选择器改为一次选择多个文件；每个附件独立上传、显示进度、失败重试和移除，发送时会同时提交全部图片与文件 ID；多文件附件可随草稿恢复，并兼容 `0.0.8` 及更早版本的单文件草稿数据；
- 实际调用模型与推理深度改为按对话分别保存，并移动到聊天页右上角；人物模型继续作为独立的全局设置，未选择人物时发送空值并由原版 Hermes 使用 `default`；模型、人物和推理深度使用三个独立选择界面，调用模型列表只显示模型名称；
- 设置页只保留人物模型及系统设置；聊天页移除右上角“新建对话”，侧栏继续提供新建入口并收窄宽度；深色模式改为 AMOLED 纯黑背景；
- 登录内容整体上移；聊天页与消息列表统一随输入法上浮，取消流式自动滚动动画，减少输入框聚焦时不必要的视觉延迟；
- 修复流式回复期间短暂显示两个助手气泡的问题；删除会话时同步清理 Nexus 自有消息缓存、草稿和会话运行配置；修复多个仅文字草稿切换后可能丢失的问题；
- 重新设计 Android 自适应应用图标，提供普通、圆形及 Android 13 单色主题资源；
- 版本升级到 `0.0.9`，生成 Android Debug APK、Gateway ZIP 和 SHA-256 清单；独立成品环境继续只运行构建后的 Gateway ZIP，不读取仓库源码、不运行 Docker。

2026-07-24 自动验证：

- Android：133 项单元测试通过，`0 failures / 0 errors / 0 skipped`；`lintDebug` 为 `0 errors, 1 warning`，唯一警告是按需求允许 HTTP 的预期 `InsecureBaseConfiguration`；Debug APK 构建成功；
- Gateway：完整测试 `82 passed, 9 skipped`；Hermes 只读边界与 Docker 配置契约聚焦回归 `8 passed`；Python `compileall` 和网页契约通过；
- 成品：`Nexus-Android-0.0.9-debug.apk` SHA-256 为 `2685adde9f7a7629e392546c097a472bf27e25b11c3544fbfda4a11b7ce8d217`；`Nexus-Gateway-0.0.9.zip` SHA-256 为 `032ff39d0c0f284db5397fe8ee953dfffbdb330aee7ced34ebeea8de7d440b6c`；
- Gateway ZIP：精确包含 20 个生产文件，完整性检查通过；不含测试、缓存、日志、运行数据、密码、Token、Hermes Key、Session Secret、Bootstrap Token、证书、私钥、APK 或 Android 构建目录；ZIP 内 Gateway 版本为 `0.0.9`；
- 独立测试环境：普通升级到 `Nexus-Gateway-0.0.9.zip` 后，`http://127.0.0.1:18787/health` 与 `http://10.0.0.123:18787/health` 均返回 200；部署记录中的 ZIP SHA-256 与成品一致；
- 端口与进程：`18787` 仅有一个绑定到 `0.0.0.0` 的 listener，PID 为 `11904`；旧 `18788` 无 listener，`process.json` PID 与实际监听进程一致；
- 数据保留：普通升级前后，Nexus 账号、上游连接配置副本、9 个媒体文件和 6 个历史 TLS 文件的存在状态、数量及内容哈希一致，未被升级清空或改写；
- 边界确认：按要求未在本机运行 Docker，未修改、安装、升级、停止、重启或以其他方式管理任何 Hermes 文件、配置、数据、虚拟环境或进程。

待验收：

- 用户在 Android 真机确认系统文件选择器可一次选取多个文件，并验证上传进度、移除、失败重试、草稿恢复及一次发送多个附件；
- 用户确认不同对话分别保存调用模型和推理深度，人物模型全局设置、AMOLED 主题、键盘上浮以及新版图标符合预期。

## 0.0.8｜移动端设置、输入体验与 Web 管理页重构

状态：功能实现、完整自动验证、成品构建、独立测试环境升级和移动 Web 验收均已完成，待用户真机与正式反向代理验收。

已完成：

- Android 新增独立全屏设置页，集中管理人物模型、实际调用模型、推理深度、定时任务、自动刷新、主题、连接信息和退出登录；模型及定时任务子页关闭后返回设置页；
- Android 使用 Material 3 重新设计登录、聊天、会话抽屉、输入区和设置界面，统一蓝紫/青绿色视觉、卡片层级、圆角和暗色主题；
- 登录页支持滚动及状态栏、导航栏和 IME 安全区，启用 `adjustResize`，软键盘不再遮挡密码栏；密码支持显隐并实时显示已输入位数；
- 聊天输入状态与整页 UI 状态解耦，草稿磁盘保存改为 350ms 防抖，并在切换会话、发送和 App 进入后台前强制落盘，减少输入法弹出和连续打字时的主线程工作；
- 人物模型与实际调用模型继续独立保留，新增可持久化的推理深度选择；手机端继续支持定时任务的新建、编辑、暂停、恢复、立即运行和删除，普通对话列表不显示定时任务执行会话；
- Android 接受显式 HTTP 或 HTTPS，裸地址默认补全 HTTP；不再强制公网 HTTPS，也不再显示证书安装提示。HTTPS 仍使用 Android 系统信任链，正式公网部署建议由反向代理提供受信任证书；同源 Bearer Token 防泄漏校验继续保留；
- Web 端改为专用初始化与运维管理页，移除聊天、会话和消息输入界面，保留概览、文件、语音、账号安全和系统状态；移动导航采用受约束的横向滚动，不再把整页撑宽；Android 使用的聊天 API 保持不变；
- 建立 Hermes 原版只读强制边界：Nexus 禁止修改任何 Hermes 文件，禁止安装、升级、降级、卸载、启动、停止、重启或终止 Hermes，只能通过原版 Hermes HTTP API 工作；测试、清理、升级和部署仅允许写入 Nexus 自有目录；
- 历史上曾在本机将 Hermes 更新至 `0.19.0` 并配置 `gpt-5.6-sol` 路由；该事实仅作记录，自 0.0.8 起禁止复现，也禁止通过再次修改 Hermes 来“恢复原状”；
- 独立成品测试环境继续位于 `成品\本地测试环境\`，只运行构建后的 Gateway ZIP，不读取源码、不运行 Docker；普通升级保留账号、Nexus 保存的 Hermes 上游连接配置副本、媒体、日志、状态和历史 `data\tls`。

2026-07-23 自动验证：

- Gateway：完整测试 `82 passed, 9 skipped`；Hermes 边界聚焦回归 `16 passed`；Python `compileall`、网页契约和 JavaScript 语法检查通过；唯一 pytest 提示为已有的未知 `asyncio_default_fixture_loop_scope` 配置警告；
- Android：Debug JVM 单元测试通过，`lintDebug` 通过，`assembleDebug` 构建成功；Lint 仅保留按需求允许明文 HTTP 的预期提示；
- 成品：`Nexus-Android-0.0.8-debug.apk` SHA-256 为 `9744ac0b7caff875a323efffcd23592b6cf9950d1614cf6e2dbb10eee803acf3`；`Nexus-Gateway-0.0.8.zip` SHA-256 为 `3211bfb21a1967e15c92bedcc533e22e543edc618e605fc92f98b7a32522b42a`；
- Gateway ZIP：共 20 个生产文件，完整性检查通过，不含测试、运行数据、缓存、日志、密码、Token、Hermes Key、Session Secret、Bootstrap Token、证书、私钥或 Android 构建目录；
- 独立测试环境：无损升级至 Gateway `0.0.8` 后，`http://127.0.0.1:18787/health` 与 `http://10.0.0.123:18787/health` 均返回 200，原版 Hermes API 状态正常；
- 端口与进程：`18787` 仅有一个 `0.0.0.0` listener，PID 为 `7252`；旧 `18788` 无 listener，`process.json` PID 与实际监听进程一致；部署记录中的 ZIP SHA-256 与成品一致；
- 数据保留：升级命令的数据保护检查通过，Nexus 账号、上游连接配置副本、媒体和历史 `data\tls` 未被普通升级清空；
- 移动 Web：在 `390×844` 视口下，导航保持单行横向滚动，`body` 与 `html` 的可滚动宽度均收敛到 `375px`，不存在整页横向溢出；导航切换正常，控制台无页面错误；
- 按要求未在本机运行 Docker，也未通过本轮操作修改或管理 Hermes 文件、安装或进程。

待验收：

- 用户在真机确认登录页键盘与密码位数、人物模型、实际调用模型、推理深度、定时任务及新版设置页交互；
- 配置正式 Nginx、Caddy 或其他 HTTPS 反向代理后，确认公网 App 与管理网页访问、证书链和流式输出。
## 0.0.7｜HTTP 源站与 HTTPS 反向代理

状态：功能实现、自动验证、成品构建和独立测试环境升级均已完成，待用户真机及正式反向代理验收。

已完成：

- Gateway 改为单一 HTTP listener，删除自动证书、临时 CA、正式证书热更新、TLS 环境变量及 `--tls-dir`；网页删除证书管理入口，并明确 HTTP 源站与反向代理职责；
- 旧 `/api/admin/tls` 路径显式停止服务，避免被通配代理转发给 Hermes；Gateway 不依据外部传入的 `X-Forwarded-*` 改写安全判断；
- Nginx/Caddy 部署文档补充 HTTPS 终止、`Host` 与 `X-Forwarded-Proto` 转发、流式响应关闭缓冲、长读取超时、HSTS 及源站端口隔离；
- Android 允许回环、私网、链路本地、CGNAT、IPv6 ULA 和 `.local` 地址使用 HTTP，同时拒绝公网 HTTP；
- 裸私网地址默认规范化为 `http://地址:18787`，裸公网域名默认规范化为 `https://域名`；旧私网 `https://地址:18788` 自动迁移回 `http://地址:18787`；
- Android 移除 Debug CA 生成、内嵌和安装流程；HTTPS 反向代理仅使用 Android 系统信任链；Bearer Token 只附加到通过校验且与 Gateway 同源的资源请求；
- 人物模型、实际调用模型、推理深度与手机端定时任务管理均保留，普通对话列表继续隐藏 `source=cron` 会话；
- 源码本地测试脚本与独立成品测试环境统一使用 HTTP `18787`；独立环境仍只运行成品 ZIP、不读取源码、不调用 Docker；
- 独立环境 upgrade 只替换 `app/` 并同步运行依赖，保留账号、Hermes 配置、媒体、运行数据和历史 `data/tls`；旧 HTTPS `18788` 必须关闭；
- Windows 防火墙只保留 LocalSubnet TCP `18787` 入站规则，公网入口由后续反向代理单独管理。

2026-07-23 自动验证：

- Gateway：`pytest` 77 项通过、9 项按环境跳过；`compileall`、网页契约测试和 JavaScript 语法检查通过；
- Android：126 项 Debug JVM 单元测试全部通过；`lintDebug` 0 errors（因按需求允许局域网 HTTP，有 1 项预期 cleartext 警告）；`assembleDebug` 构建成功；
- 成品：已生成 `Nexus-Android-0.0.7-debug.apk`（SHA-256 `f5b6f0e1fc5595ed2402b9401a782a614b3e35c4157580f773231878642f7a7c`）与 `Nexus-Gateway-0.0.7.zip`（SHA-256 `d5bbfc6b4b31a9e1c86add5b15df0070774026af35e65d71bcb6d351e8429e12`）；Gateway ZIP 文件清单完整，不含运行数据、缓存、日志、私钥、测试 CA 或 Android 构建文件；
- 独立测试环境：使用环境自身 `manage.ps1 upgrade` 从 0.0.6 无损升级到 0.0.7，本机与局域网 HTTP `/health` 均返回 200，Gateway 与 Hermes 状态正常；
- 端口与进程：HTTP `18787` 监听于 `0.0.0.0`，旧 HTTPS `18788` 无监听，`process.json` PID 与实际监听进程一致；
- 数据保留：账号、Hermes 配置、媒体和历史 `data/tls/ca.crt` 哈希保持不变；Windows 防火墙仅允许 LocalSubnet TCP `18787`。

待验收：

- 用户在手机上确认局域网 HTTP 登录、人物/调用模型、推理深度和定时任务管理；
- 配置正式 Nginx/Caddy HTTPS 域名后，确认公网 App 与网页访问、证书链和流式输出。

## 0.0.6｜推理深度、HTTPS-only 与证书热更新

状态：功能实现、自动验证、成品构建和独立测试环境升级均已完成，待用户真机验收。

已完成：

- Android 保留“人物模型”，继续独立选择实际调用模型，并新增推理深度选择、当前值展示与持久化；
- 推理深度支持默认、`none`、`minimal`、`low`、`medium`、`high`、`xhigh`、`max`、`ultra`；默认不发送字段，其余值通过 `reasoning_effort` 发送；
- Gateway 校验并透传 `reasoning_effort`。Nexus 只负责 UI、持久化、校验和透传，不声明 Hermes 或调用模型一定实际采用；
- Android 与网页普通对话列表均过滤 `source=cron` 的定时任务执行会话；手机端继续通过专用界面新建、编辑、暂停、恢复、立即运行和删除定时任务；
- Gateway 改为单一 HTTPS listener，HTTP 完全禁用，TLS 最低 1.2；
- Gateway 首次启动自动生成并持久化临时 CA/服务器证书，局域网 IP 或 SAN 变化时只重签服务器证书；现在无需提供正式证书；
- 网页“系统状态”新增证书状态和正式 PEM 证书链/未加密私钥上传，支持校验、原子替换、备份、热加载与失败回滚；正式证书可在以后取得后再配置；
- Android 禁止明文 HTTP，裸地址默认使用 HTTPS，裸局域网 IP 未写端口时补 `18788`；旧保存的本地 `http://IP:18787` 自动迁移为 `https://IP:18788`；
- Debug 构建支持通过 `NEXUS_DEBUG_CA_FILE` 内嵌成品测试环境 CA，并拒绝包含私钥的输入；Release 仍只信任系统 CA；
- 独立成品测试环境只运行 Gateway ZIP、不读取源码、不运行 Docker，只允许 LocalSubnet 访问 TCP 18788；upgrade 保留账号、Hermes 配置、媒体和整个 `data/tls`，reset 才删除 CA；
- Docker、Compose、环境变量示例和源码本地测试脚本同步为 HTTPS-only；按要求不在本机运行 Docker 测试。

2026-07-23 自动验证：

- Gateway：`pytest` 87 项通过、9 项按环境跳过；`compileall`、网页契约测试和 JavaScript 语法检查通过；
- Android：126 项 Debug JVM 单元测试全部通过，`lintDebug` 通过，`assembleDebug` 构建成功；
- 成品：生成 `Nexus-Android-0.0.6-debug.apk` 与 `Nexus-Gateway-0.0.6.zip`；APK 内嵌 CA 与独立测试环境当前 CA 完全一致且不同于源码占位 CA；Gateway ZIP 文件清单完整，不含运行数据、缓存或私钥；
- 独立测试环境：使用环境自身 `manage.ps1 upgrade` 无损升级到 0.0.6；`https://127.0.0.1:18788/health` 与 `https://10.0.0.123:18788/health` 均返回 200，Gateway 与 Hermes 状态正常；
- 端口与进程：HTTPS `18788` 监听于 `0.0.0.0`，HTTP `18787` 无监听，`process.json` PID 与实际监听进程一致；
- 数据保留：账号、Hermes 配置、媒体和 `data/tls` 保持不变；Windows 防火墙仅保留 LocalSubnet TCP `18788` 入站规则。

待验收：

- 用户在真机确认模型、推理深度、定时任务和 HTTPS 登录流程。

## 0.0.5｜Android 连接兼容与登录恢复

状态：功能实现、自动验证和独立成品测试环境升级均已完成，待用户真机验收。

已完成：

- Android 声明专用网络安全配置：Debug APK 在保留系统 CA 校验的同时信任用户主动安装的本地 CA，Release 构建仍只信任系统 CA；
- 连接页默认提示本地 App 使用 `http://电脑局域网IP:18787`，并明确 HTTPS 网页使用 `18788`；
- 裸 `IP:端口` 自动补全 HTTP 协议，地址在保存和请求前统一规范化；
- 拒绝非 HTTP(S) 协议以及包含账号信息、查询参数或锚点的服务器地址；
- 错误密码明确显示“账号或密码错误”，失效设备 Token 会清除并要求重新输入密码；
- HTTPS 证书错误会提示下载并安装 `/nexus-local-ca.crt`，同时给出可信局域网 HTTP 备用地址；
- DNS、无法连接、超时、连接中断和地址格式错误分别显示可执行的排查提示；
- 普通 API、停止回答和流式对话的非成功响应统一携带 HTTP 状态与 Gateway 错误信息。

自动验证：

- Gateway：68 项测试通过，9 项按环境条件跳过，Python `compileall` 通过；
- 网页契约：通过；
- Android：122 项 JVM 单元测试通过，0 failures / 0 errors / 0 skipped；
- Android Lint：0 issues；
- Debug APK：构建成功，生成 `Nexus-Android-0.0.5-debug.apk`；
- Gateway ZIP：生成 `Nexus-Gateway-0.0.5.zip`，SHA-256 清单已更新；
- 独立成品测试环境：通过 `upgrade` 无损升级到 0.0.5，账号、Hermes 配置、媒体和本地 HTTPS CA 保持不变；HTTP/HTTPS 健康检查、CA 下载和局域网双端口监听通过。

待真机确认：安装 0.0.5 APK 后，使用 `http://10.0.0.123:18787` 登录；可选安装本地 CA 后再验证 `https://10.0.0.123:18788`。

## 0.0.4｜人物与调用模型拆分、内置 HTTPS

状态：功能实现、自动验证和独立测试环境升级均已完成，待用户真机验收。

已完成：

- Android 将人物模型与实际调用模型拆分显示、选择和持久化；
- 人物模型继续用于角色设定，调用模型可选择 `gpt-5.6-sol` 或“使用 Hermes 默认”；
- 使用 Hermes `/v1/models` 的 `parent` 元数据区分人物模型与调用模型；
- 旧 `selected_model_id` 自动迁移为人物模型偏好；
- Android 请求分别发送 `persona_model` 与 `inference_model`；
- Gateway 兼容新旧模型字段，并按是否指定调用模型选择 OpenAI 兼容路由或原生 Session Chat 路由；
- Gateway 可同时监听 HTTP 与 HTTPS，网页 HTTP 入口可永久跳转到 HTTPS，HTTP API 继续兼容 Android；
- 独立成品测试环境使用 `18787`（HTTP API）和 `18788`（HTTPS Web），自动生成并持久保存本地 CA 与当前局域网 SAN 服务器证书；
- 提供用户主动执行的 Windows CA 安装脚本和供手机手动下载的 `/nexus-local-ca.crt`；
- 测试环境控制文件改为源码内模板维护，升级控制脚本时不会修改运行数据；
- Hermes 本地环境已配置 `gpt-5.6-sol` 模型路由，`/v1/models` 已确认“星禾”与调用模型父子关系。

自动验证：

- Gateway：68 项测试通过，9 项按环境条件跳过，Python `compileall` 通过；
- 网页契约：通过；
- Android：116 项 JVM 单元测试通过，0 failures / 0 errors / 0 skipped；
- Android Lint：无 Error；
- Debug APK：构建成功，生成 `Nexus-Android-0.0.4-debug.apk`；
- Gateway ZIP：生成 `Nexus-Gateway-0.0.4.zip`，SHA-256 清单已更新；
- HTTPS：使用本地 CA 显式校验本机和 `10.0.0.123` 的证书链、主机名与 `/health`；
- 网络：HTTP 网页 308 跳转、`0.0.0.0:18787/18788` 双监听、LocalSubnet 防火墙规则和 CA 下载一致性均通过；
- 无损升级：连续两轮 upgrade 后账号、Hermes 配置、媒体和 3 个 Hermes 定时任务保持不变，第二轮升级前后 CA SHA-256 指纹一致。

待真机确认：手机端人物/调用模型切换、HTTPS CA 安装后的移动网页访问，以及定时任务管理交互。

## 0.0.3｜移动模型选择与定时任务管理

状态：功能实现和自动验证已完成，待用户真机验收。

已完成：

- 普通对话列表过滤 `cron` 渠道，定时任务执行记录不再作为对话显示；
- 登录后非阻塞加载 Hermes 模型列表，可从聊天页菜单和设置页选择模型；
- 模型选择持久保存，发送消息时携带选定模型；
- Gateway 在指定模型时转发至 Hermes `/v1/chat/completions`，并把 OpenAI SSE 转换为移动端现有事件协议；
- 未指定模型时继续使用 Hermes 原生 Session Chat 路由；
- 手机端新增全屏定时任务管理页；
- 支持任务列表、刷新、新建、编辑、删除、暂停、恢复和立即运行；
- 表单支持 Cron、`every 2h` 固定间隔和 ISO 单次时间；
- 表单输入校验和服务器错误直接显示在编辑/删除弹窗内。

自动验证：

- Gateway：65 项测试通过，9 项按环境条件跳过；
- Android：115 项 JVM 单元测试通过，0 failures / 0 errors / 0 skipped；
- Android Lint：无 Error；
- Debug APK：构建成功。

待真机确认：模型切换后的实际回答、窄屏任务管理布局、任务 CRUD 和暂停/恢复/立即运行操作。

## 0.0.2｜安全与多会话稳定性加固

状态：源码修复和自动验证已完成，待真机验收。

已完成：

- Nexus 设备令牌仅附加到与当前网关同源的图片和下载请求，第三方 URL 不再携带认证；
- HTTP 登录增加明文风险确认，保留可信局域网 HTTP 自用能力；
- 首次初始化改为一次性 Bootstrap Token，成功后自动删除；配置或账号文件损坏时 fail closed；
- 网关管理员密码改为 scrypt 加盐哈希，旧明文账号在成功登录后自动迁移；
- 登录按来源 IP 限速；
- 多个会话可同时进行本机后台状态监控；
- 登出只取消 App 本机流、上传、下载、监控和通知，不停止 Hermes 服务端任务；
- 会话缓存键改为稳定编码，通知 ID 与 PendingIntent 按类型、会话和文件隔离；
- OpenDocument 文件取得持久读取权限，不支持时复制到 App 私有缓存；
- SSE 使用 Gson 标准 JSON 解析，支持 Unicode、Emoji、反斜杠和多行 data；
- 回答和文件通知默认使用锁屏隐私模式；
- 网关增加上传总配额、磁盘低水位、绝对路径隐藏和错误脱敏；
- Gradle Wrapper 增加官方 SHA-256 校验。

自动验证：

- Gateway：41 项测试通过，Python 编译检查通过；
- 网页契约：通过；
- Android 临时 ASCII 副本：99 项 JVM 测试通过，0 failures / 0 errors / 0 skipped；
- Android Lint：无 Error；
- Debug APK：构建成功，生成约 17.6 MB 安装包；
- GitHub CI：待推送后确认。

待真机确认：HTTP 风险弹窗、多会话后台通知、登出后的服务器任务续跑、附件重启恢复和外部 URL 下载行为。

## 0.0.1｜首个 Nexus 开源版本

- 完成 Nexus 品牌、包名 `app.nexus.mobile` 与 `nexus_gateway` 网关统一；
- 提供 Android 多会话、附件、下载、通知、草稿和长会话分页；
- 提供账号认证、Hermes 会话代理、SSE、媒体和 Web 管理；
- 提供 Docker、NAS 部署、CI、许可证、安全与贡献文档；
- 星禾移动端 v0.0.20 冻结为历史最终版，后续开发统一进入 Nexus。
